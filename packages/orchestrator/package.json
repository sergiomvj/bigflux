{
  "name": "@bigflux/orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "XState state machine, gates, GateResult",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --build"
  }
}
