{
  "name": "@bigflux/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "barramento Realtime (big_flux.approved ...)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --build"
  }
}
