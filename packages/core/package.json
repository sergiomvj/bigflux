{
  "name": "@bigflux/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "tipos compartilhados (Issue, GateResult, Result-like)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --build"
  }
}
