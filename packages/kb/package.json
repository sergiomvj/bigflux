{
  "name": "@bigflux/kb",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Knowledge Base pgvector (global+tenant)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --build"
  }
}
