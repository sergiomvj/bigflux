{
  "name": "@bigflux/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Supabase Auth wrapper, RBAC guards, papel approver",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --build"
  }
}
