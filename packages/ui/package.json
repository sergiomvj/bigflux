{
  "name": "@bigflux/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DS v2 tokens + componentes React + Storybook",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --build"
  }
}
