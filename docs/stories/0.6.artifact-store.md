# Story 0.6: Artifact store — geração, versionamento e exportação

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [test, typecheck, lint]

## Story Points

5 (T-shirt: M) — store + versionamento + exportadores multi-formato.

## Story

**As a** usuário,
**I want** baixar, regenerar, duplicar, favoritar e versionar qualquer artefato gerado,
**so that** eu reuse o resultado profissional.

## Acceptance Criteria

1. Given um artefato gerado, When salvo, Then aparece em `generated_artifacts` com `version=1`, mime, tamanho, formato e metadados.
2. Given edição/regeneração, When confirmada, Then cria `artifact_versions` com `change_summary`, preservando histórico.
3. Given artefato visual, When exibido, Then mostra preview antes do download e tela de `celebrate`.
4. Given storage, When arquivo salvo, Then segue o padrão de path e respeita `max_file_storage_mb` do plano.

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: API · Secondary: Frontend · Complexity: Medium.
**Specialized Agents** — Primary: @dev · Supporting: @ux-design-expert.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — storage path convention, version history integrity, RLS, quota `max_file_storage_mb`.

## Tasks / Subtasks

- [ ] Schema (AC: 1, 2)
  - [ ] `generated_artifacts`, `artifact_versions` (RLS por tenant)
- [ ] Service (AC: 1, 2, 4)
  - [ ] `ArtifactService` (create/version/export/favorite/duplicate)
  - [ ] Path: `{app_slug}/{tenant_id}/{yyyy}/{mm}/{run_id}/{artifact_slug}.{ext}`; respeita `max_file_storage_mb`
- [ ] Exportadores (AC: 1)
  - [ ] PDF/DOCX/CSV/MD/ZIP; integração Supabase Storage
- [ ] Preview (AC: 3)
  - [ ] Preview server-side; tela `celebrate` (DS v2 de S0.3) na entrega

## Dev Notes

- **Fonte:** sprint-00 S0.6; software-premium-params §11.7-11.8/§13; README §6.5 (artefato-primeiro).
- **Pilares:** P5 (versionamento).
- **Formatos:** Big Flux ⇒ MD+JSON+PDF; copy/briefing/relatório ⇒ MD/CSV/PDF/ZIP.
- **Depende de S0.5** (`max_file_storage_mb` via `user_premium_limits`/quota) e **S0.3** (`celebrate`, `prog-friendly`).
- **DS v2:** `celebrate` na entrega, `prog-friendly` na geração.

### Testing

- Export multi-formato testado. Versionamento preserva histórico. Preview funciona.
- Cobertura ≥90%.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.6 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 9/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
