# Story 0.4: Infra Premium — provedores, configs de modelo e router multi-LLM

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [test, typecheck, lint]

## Story Points

8 (T-shirt: L) — router por capacidade + cascata de fallback + secrets.

## Story

**As a** Dev,
**I want** um router que escolhe modelo por capacidade e tarefa com fallback,
**so that** cada agente use o modelo certo sem hardcode.

## Acceptance Criteria

1. Given config `quality_first`, When o Big Flux Architect é invocado, Then o router seleciona Opus 4.7 e registra a escolha.
2. Given o modelo principal falha (timeout), When o router executa fallback, Then tenta equivalente → provedor alt → reduz complexidade, e cada tentativa grava em `premium_llm_calls`.
3. Given um agente que exige `json_schema`, When um provedor sem essa capacidade é candidato, Then é descartado pelo router.
4. Given uma key, When qualquer log é gravado, Then a key nunca aparece (apenas `provider_slug`/`secret_ref`).

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: API · Secondary: Security · Complexity: High.
**Specialized Agents** — Primary: @dev · Supporting: @architect.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — secrets management (sem key em log/prompt), error handling/retry-backoff, schema validation de I/O, capability matching.

## Tasks / Subtasks

- [ ] Schema (AC: 1, 4)
  - [ ] Tabelas `premium_model_providers`, `premium_model_configs` (software-premium-params §11.1-11.2) com RLS
- [ ] Router (AC: 1, 3)
  - [ ] `ModelRouter.select(taskCapabilities, profile)` — seleciona por capacidade (`json_schema`, `function_calling`, `long_context`), não por nome
  - [ ] Perfis: `cost_saver|balanced|quality_first|speed_first|multimodal_first|legal_safe`
- [ ] Client + fallback (AC: 2)
  - [ ] `LLMClient.invoke()` com retry/backoff e cascata: equivalente → provedor alt → reduz complexidade
  - [ ] Cada tentativa grava `premium_llm_calls`
- [ ] Prompt-contract runner (AC: 3)
  - [ ] System + JSON schema validável
- [ ] Secrets (AC: 4)
  - [ ] Secrets via env por ambiente; `provider_slug` + `secret_ref`; nunca em `input_payload`/logs

## Schema

Ver `prd/software-premium-params.md` §11.1-11.2 (`premium_model_providers`, `premium_model_configs`).

## Dev Notes

- **Fonte:** sprint-00 S0.4; software-premium-params §7 (multi-LLM), §10 (prompt contracts), §11.1-11.2.
- **Pilares:** P5 (versionamento/log), P6 (adapters/provedores plugáveis).
- **Mapa modelo→perfil (README §6.1):** Architect=opus quality_first; Estrategista/Copy/Diretor/Analista/Otimizador=sonnet balanced; Auditor/Sentinela=haiku speed/cost; ORACULO=opus quality_first streaming.
- Anthropic é default; OpenAI/Gemini plugáveis. Capacidades decidem, não nomes (§7.4).
- **Depende de S0.1** (RLS nas tabelas premium) e **S0.0** (tooling).

### Testing

- Fallback testado com mock de falha (timeout). Seleção por capacidade coberta.
- Teste: nenhuma key aparece em log gravado. Cobertura ≥90%.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.4 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 9/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
