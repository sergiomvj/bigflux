# Story 0.9: Camada de Adapters — interface comum + skeleton Meta/Google

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [test, typecheck, lint]

## Story Points

5 (T-shirt: M) — interface + skeletons + resiliência (retry/circuit breaker).

## Story

**As a** Dev,
**I want** adapters intercambiáveis com interface única,
**so that** agentes nunca dependam da API específica da plataforma.

## Acceptance Criteria

1. Given um agente, When publica/consulta campanha, Then chama apenas `TrafficPlatformAdapter` — nunca Meta/Google API direto.
2. Given erro transitório, When ocorre, Then retry exponencial; se API fora, circuit breaker abre.
3. Given erro de plataforma, When retornado, Then é mapeado para `AdapterResult.error.code` padronizado entre adapters.
4. Given troca de adapter, When feita, Then nenhum código de agente muda.

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: Integration · Secondary: Architecture · Complexity: Medium.
**Specialized Agents** — Primary: @dev · Supporting: @architect.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — interface stability, circuit breaker/retry correctness, error code normalization entre adapters, sem acoplamento agente→API.

## Tasks / Subtasks

- [ ] Interface (AC: 1, 4)
  - [ ] `TrafficPlatformAdapter`; `AdapterResult<T>`
- [ ] Skeletons (AC: 1)
  - [ ] `MetaAdapter`/`GoogleAdapter` (stubs + sandbox) — implementação completa fica em S13.D1/D2
- [ ] Resiliência (AC: 2, 3)
  - [ ] Retry exponencial; circuit breaker; fila idempotente; mapa de erros padronizado

## Schema / contrato

`TrafficPlatformAdapter` e `AdapterResult<T>` conforme Parte 2 §5.3/§8.5.

## Dev Notes

- **Fonte:** sprint-00 S0.9; Parte 2 §5.3/§8.
- **Pilares:** P6 (adapters intercambiáveis).
- **Escopo:** só skeleton + resiliência + erro padronizado. CRUD real Meta/Google = S13.D1/D2.
- **Único agente que chama adapter:** Configurador (S13.C4). Aqui não há agente ainda — só a camada.
- **Depende de S0.7** (orquestrador; fila idempotente reusa eventos/estado).

### Testing

- Stub testado em sandbox; circuit breaker validado; erros normalizados.
- Cobertura ≥90%.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.9 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 8/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
