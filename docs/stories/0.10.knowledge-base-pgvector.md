# Story 0.10: Knowledge Base com pgvector (global + tenant) e eventos de domínio

## Status

Ready

## Executor Assignment

executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: [test, typecheck, lint]

## Story Points

5 (T-shirt: M) — pgvector + escopo global/tenant + barramento de eventos.

## Story

**As a** agente,
**I want** ler de uma KB com escopo global e tenant,
**so that** fundamentar decisões com aprendizados sem quebrar isolamento.

## Acceptance Criteria

1. Given KB com `scope='global'`, When qualquer tenant lê, Then acessa; `scope='tenant'` só o próprio tenant.
2. Given uma busca semântica, When executada, Then usa embedding e respeita escopo.
3. Given um evento `big_flux.approved`, When emitido, Then assinantes (cache do tráfego) recebem.

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: Database · Secondary: Integration · Complexity: Medium.
**Specialized Agents** — Primary: @data-engineer · Supporting: @dev.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — pgvector index, RLS + CHECK de escopo (global vs tenant), event delivery, sem leak cross-tenant em busca semântica.

## Tasks / Subtasks

- [ ] Schema (AC: 1, 2)
  - [ ] Tabela `knowledge_base` (Parte 3 §6.2) com pgvector + RLS + CHECK de escopo (`global`/`tenant`)
- [ ] Embedding (AC: 2)
  - [ ] Serviço de embedding; busca semântica que respeita escopo
- [ ] Eventos (AC: 3)
  - [ ] Barramento de eventos Realtime (`big_flux.approved`, `campaign.published`, `contingency.detected`)

## Dev Notes

- **Fonte:** sprint-00 S0.10; Parte 3 §6.2 (KB global+tenant); README §7 (eventos de domínio).
- **Pilares:** P4 (isolamento na KB tenant), P5 (eventos/log).
- **Escopo:** `global` = mantida por produto, lida por todos; `tenant` = aprendizados do business, RLS estrito.
- **`big_flux.approved`** é consumido pelo cache de `TrafficConstraints` (S13.A1) — barramento nasce aqui.
- **Depende de S0.1** (RLS). Requer extensão `pgvector` no Supabase.

### Testing

- Busca semântica testada; escopo respeitado (global lê, tenant isola); eventos entregues.
- Teste de leak: busca de tenant A nunca retorna `scope=tenant` de B. Cobertura ≥90%.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.10 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 8/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
