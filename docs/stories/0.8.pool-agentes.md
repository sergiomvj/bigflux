# Story 0.8: Pool de execução de agentes — envelope, logging e 3 modos de invocação

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [test, typecheck, lint]

## Story Points

8 (T-shirt: L) — envelope tipado + 3 modos (sync/job/stream) + catálogo de erros.

## Story

**As a** Dev,
**I want** invocar qualquer agente por um envelope tipado com log obrigatório,
**so that** padronizar e auditar todas as execuções de IA.

## Acceptance Criteria

1. Given uma invocação, When executada, Then segue `AgentInvocation`/`AgentResponse` e gera registro em `agent_executions`.
2. Given um agente longo (Architect/Analista), When invocado em modo job, Then retorna `job_id` e suporta polling/webhook.
3. Given ORACULO, When invocado, Then responde via streaming (SSE/WebSocket).
4. Given um erro, When ocorre, Then retorna `AgentError` com código do catálogo (`MODEL_TIMEOUT`, `MISSING_CONTEXT`, `BLAST_RADIUS_EXCEEDED`…) e `retryable` correto.
5. Given agentes operacionais, When um precisa de outro, Then a comunicação passa pelo orquestrador (nunca agente→agente).

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: API · Secondary: Architecture · Complexity: High.
**Specialized Agents** — Primary: @dev · Supporting: @architect.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — logging 100% (nenhuma execução sem registro), error catalog completo, statelessness, sem chamada agente→agente.

## Tasks / Subtasks

- [ ] Tipos (AC: 1, 4)
  - [ ] `AgentInvocation`/`AgentResponse`/`AgentError`; catálogo de erros (`MODEL_TIMEOUT`/`MISSING_CONTEXT`/`BLAST_RADIUS_EXCEEDED`…)
- [ ] Runner (AC: 2, 3)
  - [ ] `AgentRunner` 3 modos: sync, job (`job_id` + polling/webhook), stream (SSE/WebSocket)
- [ ] Logging (AC: 1, 5)
  - [ ] Tabela `agent_executions` (Parte 3 §3.6) com RLS; versionamento `agent_version`/prompt
  - [ ] Comunicação inter-agente só via orquestrador (S0.7)

## Schema

`agent_executions` conforme Parte 3 §3.6 (com `tenant_id` + RLS).

## Dev Notes

- **Fonte:** sprint-00 S0.8; Parte 3 §3 (protocolo de agentes); software-premium-params §8.
- **Pilares:** P5 (log/versão), P6 (envelope padrão).
- **Stateless:** agentes não guardam estado entre invocações; contexto vem no envelope.
- **Streaming (AC3)** habilita ORACULO (S14) — primeiro token <2s é meta de S14, aqui só o canal.
- **Depende de S0.4** (`LLMClient`/router) e **S0.7** (orquestrador invoca; inter-agente passa por ele).

### Testing

- 3 modos testados; logging 100%; catálogo de erros coberto; statelessness verificada.
- Cobertura ≥90%.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.8 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 9/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
