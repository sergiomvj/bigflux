# Story 0.7: Traffic Orchestrator base — state machine + gates determinísticos genéricos

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [test, typecheck, lint]

## Story Points

8 (T-shirt: L) — engine de state machine + registry de gates + eventos.

## Story

**As a** Dev,
**I want** um orquestrador de código (não-agente) que encadeia etapas, aplica gates booleanos e persiste estado,
**so that** o fluxo seja determinístico e auditável.

## Acceptance Criteria

1. Given um workflow declarado, When uma etapa conclui, Then o estado transiciona só se o gate `passed=true` (sem issue `block`).
2. Given um gate com issue `warn`, When humano confirma, Then avança; sem confirmação, fica retido.
3. Given falha de etapa, When ocorre, Then o orquestrador faz retry/escalation/rollback conforme política e emite evento.
4. Given qualquer transição, When acontece, Then é persistida (auditoria).

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: Architecture · Secondary: API · Complexity: High.
**Specialized Agents** — Primary: @dev · Supporting: @architect.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — gates 100% determinísticos (nenhum LLM), cobertura de transições, event emission, idempotência.

## Tasks / Subtasks

- [ ] Engine (AC: 1, 2, 4)
  - [ ] State machine própria ou XState; persistência de estado
  - [ ] Tipos `GateResult`/`Issue`; registry de gates por etapa
- [ ] Resiliência (AC: 3)
  - [ ] Política retry/escalation/rollback; emissor de eventos (Supabase Realtime)
- [ ] Testes (AC: 1-4)
  - [ ] Testes determinísticos de gate (cobertura ≥90%)

## Schema / contrato

```typescript
interface Issue { level: 'block'|'warn'|'info'; msg: string; affected_element?: string; suggested_fix?: string; }
interface GateResult { passed: boolean; issues: Issue[]; }
type GateFn<TCtx> = (ctx: TCtx) => GateResult; // determinístico, sem LLM
```

## Dev Notes

- **Fonte:** sprint-00 S0.7; Parte 2 §5.2 (orquestrador = código puro), §4.3 (gate genérico); Parte 3 §3.1 (agentes nunca se chamam; só orquestrador invoca).
- **Pilares:** P2 (validação determinística), P3 (HITL no `warn`), P5 (auditoria).
- **CRÍTICO:** gates são código puro — **nunca LLM** (P2). LLM só onde há criatividade/semântica.
- Base reusada por S0.8 (pool de agentes), S0.9 (adapters), e a state machine das 10 etapas em S13.B1.
- **Depende de S0.1** (persistência de estado com RLS).

### Testing

- State machine testada; gates 100% determinísticos; eventos emitidos.
- Cobertura ≥90% (P2 gate). Local `packages/orchestrator`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.7 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 9/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
