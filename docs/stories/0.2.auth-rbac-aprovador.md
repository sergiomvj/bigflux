# Story 0.2: Autenticação, RBAC e papel "Aprovador do Big Flux"

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [test, typecheck, lint]

## Story Points

5 (T-shirt: M) — integração Supabase Auth + guards por role.

## Story

**As a** Owner do tenant,
**I want** definir quem aprova o Big Flux e quem gerencia tráfego,
**so that** decisões críticas tenham identidade registrada (HITL/P3 depende de identidade).

## Acceptance Criteria

1. Given roles definidas, When um `viewer` tenta aprovar Big Flux, Then a ação é bloqueada com mensagem amigável.
2. Given um `approver`, When aprova um Big Flux, Then `approved_by` e `approved_at` são gravados.
3. Given sessão expirada, When usuário age, Then é redirecionado a login sem expor jargão técnico.

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: Security · Secondary: API · Complexity: Medium.
**Specialized Agents** — Primary: @dev · Supporting: @architect.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — authentication/authorization, input validation, sem leak de jargão técnico em erro, sessão segura.

## Tasks / Subtasks

- [ ] Auth (AC: 3)
  - [ ] Integração Supabase Auth; sessão + refresh
  - [ ] Redirect amigável em sessão expirada (sem termo técnico — DS v2 microcopy)
- [ ] RBAC (AC: 1, 2)
  - [ ] Guard de rota por role (`owner/approver/traffic_manager/viewer/super_admin`)
  - [ ] Middleware injeta `tenant_id` + role no contexto de request (chama `set_tenant_context` de S0.1)
  - [ ] Persistir `approved_by`/`approved_at` na aprovação
- [ ] Testes (AC: 1, 2, 3)
  - [ ] Matriz de permissões por role; teste de sessão expirada

## Dev Notes

- **Fonte:** sprint-00 S0.2; Parte 1 §13 (usuário aprovador por tenant); README §5 (papéis).
- **Pilares:** P3 (HITL/identidade), P4 (tenant).
- **Depende de S0.1:** middleware deve setar `app.current_tenant_id` via `set_tenant_context()` antes de qualquer query (senão RLS retorna vazio).
- Microcopy de erro: nunca expor "HTTP 401/token expired" — DS v2 `friendly-error` (componente vem em S0.3; aqui usar texto amigável placeholder).

### Testing

- Matriz role × ação (aprovar/gerenciar/ler). Teste de autorização por papel.
- Local `packages/`. Vitest.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.2 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 8/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
