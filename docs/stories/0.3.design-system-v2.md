# Story 0.3: Design System v2 tokenizado e biblioteca de componentes base

## Status

Ready

## Executor Assignment

executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: [test, lint, a11y]

## Story Points

8 (T-shirt: L) — ~12 componentes + tokens + Storybook + a11y.

## Story

**As a** Frontend,
**I want** tokens e componentes do DS v2 prontos,
**so that** toda tela seja mobile-first e consistente sem reinventar estilos.

## Acceptance Criteria

1. Given os tokens CSS do DS v2, When um componente é estilizado, Then usa apenas variáveis (`--red`, `--grad-brand`, `--r-2xl`, `--tap`…), nunca hex hardcoded.
2. Given mobile (≤560px), When qualquer tela renderiza, Then CTA é alcançável com o polegar e toque ≥48px.
3. Given a biblioteca, When dev usa `<Stepper>`, `<Coach>`, `<Dropzone>`, `<TemplateCard>`, `<FriendlyError>`, `<Celebrate>`, `<ProgressFriendly>`, Then o visual é idêntico ao DS v2.
4. Given um erro de sistema, When exibido, Then nunca aparece termo técnico cru ("HTTP 429"), e sim mensagem + próximo passo.

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: Frontend · Secondary: Architecture · Complexity: High.
**Specialized Agents** — Primary: @ux-design-expert · Supporting: @dev.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — WCAG 2.1 AA (semantic HTML, ARIA, keyboard nav), tap-target ≥48px, mobile-first breakpoints, "no hardcoded color" lint.

## Tasks / Subtasks

- [ ] Tokens & fontes (AC: 1)
  - [ ] Importar Bricolage Grotesque (display), Inter (corpo), JetBrains Mono (dados)
  - [ ] Arquivo de tokens CSS (superfícies, texto, acentos, gradientes, raios, sombras, tap)
  - [ ] Lint rule "no hardcoded color" (ativada no eslint de S0.0)
- [ ] Componentes (AC: 2, 3, 4)
  - [ ] `Button` (`btn-grad`/`btn-cta`/`btn-ghost`), `Stepper`, `Coach`, `Dropzone`, `TemplateCard`, `CaptionFrame`, `ProgressFriendly`, `FriendlyError`, `Celebrate`, `Field`, `Toggle`, `Badge`
- [ ] Qualidade (AC: 2, 3)
  - [ ] Storybook publicado
  - [ ] Teste de acessibilidade/tap-target; snapshot mobile

## Dev Notes

- **Fonte:** sprint-00 S0.3; `prd/design-system-v2.html` (fonte canônica de tokens); README §5.
- **Pilares:** transversal de qualidade.
- **Tokens canônicos** (README §5): `--bg:#0D0D0D`, `--surface:#171717`, `--text:#F0EDE6`, `--red:#FF3D57`, `--green:#1FBA7A`, `--grad-brand:linear-gradient(135deg,#FF3D57,#FF8C00)`, `--r-2xl:22px`, `--tap:48px`, `--tap-cta:54px`. Ler valores completos do HTML antes de codar.
- **Princípios DS v2:** mobile-first, uma decisão por tela, ensina pelo caminho, ousadia só em CTA/entrega, erro é direção.
- `FriendlyError` é consumido por S0.2 (sessão expirada) e todas as fases.

### Testing

- a11y: jest-axe / Storybook a11y addon. Tap-target ≥48px verificado.
- Snapshot mobile (≤560px). Local `packages/ui`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.3 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 9/10), Status Draft→Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
