# Story 0.1: Modelo multi-tenant com RLS e isolamento comprovado

## Status

Done

## Executor Assignment

executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: [test, typecheck, lint]

## Story Points

8 (T-shirt: L) — RLS em todas as tabelas + suite de isolamento parametrizada.

## Story

**As a** Dev,
**I want** um modelo de tenant com RLS forçado em todas as tabelas,
**so that** nenhum dado vaze entre businesses — mesmo em bug ou debug.

## Acceptance Criteria

1. Given duas tenants A e B com dados, When um usuário de A consulta qualquer tabela de domínio, Then só retornam linhas com `tenant_id = A`.
2. Given RLS ativo, When um dev tenta query sem `app.current_tenant_id` setado, Then a query retorna vazio (fail-closed), não erro silencioso que exponha dados.
3. Given um super-admin, When acessa dashboard consolidado, Then vê agregados cross-tenant read-only e a ação fica em log de auditoria.
4. Given um teste automatizado de isolamento, When roda no CI, Then prova que nenhuma das N tabelas permite leak cross-tenant.

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: Database · Secondary: Security · Complexity: High.
**Specialized Agents** — Primary: @data-engineer · Supporting: @dev, @architect.
**Quality Gates** — [x] Pre-Commit (@dev) · [x] Pre-PR (@devops).
**Focus Areas** — RLS coverage em 100% das tabelas, fail-closed policy, sem `tenant_id` faltando em query, migration safety, audit log cross-tenant.

## Tasks / Subtasks

- [x] Migrations base (AC: 1, 3)
  - [x] `tenants`, `users`, `tenant_memberships(role)` com RBAC (CHECK role IN owner/approver/traffic_manager/viewer/super_admin)
- [x] Contexto de tenant (AC: 1, 2)
  - [x] Função `set_tenant_context(uuid)` que seta `app.current_tenant_id`
  - [x] Template/macro de policy `tenant_isolation` aplicável a toda tabela de domínio
- [x] Super-admin (AC: 3)
  - [x] View materializada agregada cross-tenant (read-only)
  - [x] `audit_log` de acesso cross-tenant
- [x] Testes (AC: 4)
  - [x] Suite `tenant-isolation.spec` parametrizada por tabela (roda no CI)

## Schema

```sql
CREATE TABLE tenants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL, segment TEXT, business_model TEXT, size_estimate TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE TABLE tenant_memberships (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id),
  user_id UUID NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('owner','approver','traffic_manager','viewer','super_admin')),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE (tenant_id, user_id)
);
-- macro aplicado a toda tabela de domínio:
-- ALTER TABLE <t> ENABLE ROW LEVEL SECURITY;
-- CREATE POLICY tenant_isolation ON <t>
--   USING (tenant_id = current_setting('app.current_tenant_id')::uuid);
```

## Dev Notes

- **Fonte:** sprint-00 S0.1; Parte 2 §2.1 (tenant isolation "sagrado"); README §7 (convenção de banco).
- **Pilares:** P4 (multi-tenant), P5 (log/auditoria).
- Toda tabela de domínio das stories seguintes herda o macro `tenant_isolation` desta story.
- **fail-closed:** sem `app.current_tenant_id` → policy retorna 0 linhas, nunca erro que exponha schema/dados.
- Supabase: usar `auth.uid()`/JWT claim p/ derivar tenant em S0.2; aqui foca no GUC `app.current_tenant_id`.

### Testing

- `tenant-isolation.spec`: parametrizada — itera todas as tabelas de domínio registradas e prova zero leak A↔B.
- Cobertura RLS-relevante ≥90% (P4 gate). Roda no CI (depende de S0.0).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada a partir de sprint-00 S0.1 | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 9/10), Status Draft→Ready | @po (Pax) |
| 2026-06-26 | 0.3 | Implementadas migrations 0001 e 0002, DatabaseContext e testes | @data-engineer |

## Dev Agent Record

### Agent Model Used
Gemini 3.5 Flash (Low) / AntiGravity.

### Debug Log References
- Nenhuma falha encontrada. Lint, typecheck e testes rodados localmente via turbo executaram sem erros.

### Completion Notes List
- Migrations `0001_initial_schema_and_rls.sql` e `0002_tenant_helpers.sql` criadas.
- Implementação de `DatabaseContext` no index do package db com suporte a RPC set/clear e auditoria de ações.
- Criados os testes unitários de integração e isolamento de RLS (`tenant-isolation.spec.ts` e `index.spec.ts`).

### File List
- `packages/db/src/migrations/0001_initial_schema_and_rls.sql`
- `packages/db/src/migrations/0002_tenant_helpers.sql`
- `packages/db/src/index.ts`
- `packages/db/src/index.spec.ts`
- `packages/db/src/tenant-isolation.spec.ts`

## QA Results
Pendente validação formal do QA. Testes verdes localmente.
