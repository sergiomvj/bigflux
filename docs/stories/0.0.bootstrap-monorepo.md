# Story 0.0: Bootstrap do monorepo e tooling (lint/typecheck/test/CI)

## Status

Done

## Executor Assignment

executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, test, ci]

## Story Points

5 (T-shirt: M) — fundação de tooling, sem lógica de domínio.

## Story

**As a** Dev/DevOps,
**I want** um monorepo TypeScript com lint, typecheck, test e CI configurados,
**so that** todas as demais stories (S0.1+) tenham `npm run lint/typecheck/test` funcionando — o DoD global depende disso e nenhuma outra story o entrega.

## Acceptance Criteria

1. Given o repositório, When clono e rodo `npm install`, Then o workspace monorepo instala todas as deps sem erro.
2. Given o código, When rodo `npm run lint`, `npm run typecheck`, `npm run test`, Then cada comando executa (mesmo com 0 testes de domínio) e retorna exit 0.
3. Given um push/PR, When o CI dispara, Then roda lint + typecheck + test e bloqueia merge em falha.
4. Given a estrutura, When um dev cria um package em `packages/`, Then ele herda tsconfig base, eslint e jest/vitest sem reconfigurar.
5. Given absolute imports (Constitution Art. VI), When um módulo importa outro, Then usa path alias (`@bigflux/*`), não caminho relativo profundo.

## 🤖 CodeRabbit Integration

**Story Type Analysis** — Primary: Deployment · Secondary: Architecture · Complexity: Medium.
**Specialized Agents** — Primary: @devops · Supporting: @architect.
**Quality Gates** — [ ] Pre-Commit (@dev) · [ ] Pre-PR (@devops).
**Focus Areas** — CI pipeline config, secrets (sem credencial hardcoded), env var usage, tsconfig/eslint consistency.

## Tasks / Subtasks

- [x] Estrutura monorepo (AC: 1, 4)
  - [x] `package.json` raiz com workspaces (`packages/*`) — escolhido **pnpm workspaces** (`pnpm-workspace.yaml`: `packages/*`, `apps/*`)
  - [x] `tsconfig.base.json` com `paths` (`@bigflux/*`) e strict mode (+ `noUncheckedIndexedAccess`)
  - [x] ESLint flat config + Prettier; regra "no hardcoded color" reservada p/ S0.3 (comentada/desligada em `eslint.config.js`)
- [x] Scripts (AC: 2)
  - [x] `lint`, `typecheck`, `test`, `build` na raiz via **Turborepo 2** (`turbo.json`) — todos exit 0
  - [x] Test runner **Vitest 2** + coverage v8 configurado (`vitest.config.ts`; gate ≥90% só de S0.1+)
- [x] CI (AC: 3)
  - [x] `.github/workflows/ci.yml`: install → lint → typecheck → test (Node 20, pnpm)
  - [x] Branch protection documentada (`docs/framework/ci.md` — required check `ci`; aplicação é @devops)
- [x] Convenções (AC: 5)
  - [x] `docs/framework/coding-standards.md`, `tech-stack.md`, `source-tree.md` (já criados por @architect; devLoadAlwaysFiles OK)
  - [x] Lint rule `no-restricted-imports` p/ forçar absolute imports (verificado: bloqueia `../../`)

## Dev Notes

- **Fonte:** README §9 (DoD global exige `lint/typecheck/test` + cobertura ≥90% gates), Constitution Art. VI (Absolute Imports SHOULD).
- **core-config:** `devLoadAlwaysFiles` aponta `docs/framework/{coding-standards,tech-stack,source-tree}.md` — criar nesta story senão @dev falha load.
- **Stack FECHADA (@architect, ver `docs/framework/tech-stack.md`):** TypeScript 5.5 strict sobre Node 20 LTS · **pnpm workspaces + Turborepo 2** (monorepo) · **Vitest 2 + coverage-v8** (test) · **ESLint 9 flat + Prettier 3** (lint/format) · **GitHub Actions** (CI). Frontend = **Vite 5 + React 18** (não Next — ver ADR-0002). DB/infra = **Supabase** (Postgres 15 + RLS + Auth + Storage + Realtime + pgvector — ADR-0001). State machine = **XState 5** (S0.7). LLM SDK default = **@anthropic-ai/sdk** + provedores plugáveis lazy. Validação = **Zod 3**.
  - **Escopo S0.0:** instalar SÓ o tooling de fundação (TS, pnpm/turbo, eslint/prettier, vitest) + esqueleto de packages com 1 smoke test cada. **NÃO** instalar Supabase SDK/React/Vite/XState/LLM SDKs/Storybook nesta story — entram nas stories donas (S0.1+). Ver `docs/framework/tech-stack.md` §4 e `docs/framework/source-tree.md`.
  - **Frontend não bloqueia a S0.0:** o `apps/web` (Vite) entra em S0.3; a S0.0 só precisa do workspace + tooling verde.
- **Boundary:** `boundary.frameworkProtection: false` neste projeto — `packages/` é L4, livre.
- Não implementa lógica de domínio. Só esqueleto + tooling.

### Testing

- Local: `packages/`. Test runner Vitest; arquivos `*.spec.ts` co-localizados.
- Esta story entrega 1 teste smoke por package skeleton p/ provar pipeline verde.
- Cobertura: gate ≥90% só vale a partir de stories com lógica (S0.1+).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | 0.1 | Story criada (bootstrap; pré-requisito de toda S0) | @sm (River) |
| 2026-06-23 | 0.2 | Validada @po — GO (score 9/10), Status Draft→Ready | @po (Pax) |
| 2026-06-23 | 0.3 | Stack fechada por @architect (ver docs/framework/tech-stack.md) | @architect (Aria) |
| 2026-06-23 | 0.4 | Implementada (bootstrap) — Ready→InProgress→InReview | @dev (Dex) |
| 2026-06-23 | 0.5 | QA gate: PASS — Status InReview→Done | @qa (Quinn) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (@dev / Dex), YOLO autonomous mode.

### Debug Log References

- Typecheck initial failure: smoke tests imported via `@/index` alias which did not
  resolve under per-package `tsconfig` `extends`. **Fix:** switched smoke imports to
  raw-relative `./index` (allowed by coding-standards §2 for same-dir) and removed the
  unused `@/` path + per-package vitest alias. Re-ran: typecheck 12/12 green.
- Turbo `test` task warned "no output files" because default `vitest run` emits no
  `coverage/**`. **Fix:** moved coverage outputs to a separate `test:coverage` turbo
  task; default `test` declares no outputs (coverage gate is S0.1+ only).

### Completion Notes List

**Stack installed (foundation ONLY — Const. Art. IV / tech-stack §4):** TypeScript 5.9,
pnpm 10.26 + Turborepo 2.9, ESLint 9.39 flat + typescript-eslint 8.62 + Prettier 3.8,
Vitest 2.1 + @vitest/coverage-v8. **NOT installed** (deferred to owner stories S0.1+):
Supabase SDK, React/Vite, XState, LLM SDKs, Storybook, PDF libs.

**Decisions ([AUTO-DECISION]):**
- Monorepo: pnpm workspaces + Turborepo (per tech-stack §2.2). `apps/*` + `packages/*` globs.
- `apps/web` reserved as a README-only placeholder (no `package.json`) → pnpm/turbo ignore
  it, so no frontend deps leak into S0.0. Real app lands in S0.3 (ADR-0002).
- Node `engines` set to `>=20.11` (non-strict). Local toolchain is Node 24 (works for all
  tooling); CI pins Node 20 LTS per tech-stack §2.1.
- 12 package skeletons created per source-tree §5 (core, db, auth, ui, llm-router, cost,
  artifacts, orchestrator, agents, adapters, kb, events), each with `package.json`,
  `tsconfig.json` (extends base), `vitest.config.ts` (re-exports base), `src/index.ts`
  (typed marker, no domain logic), and 1 co-located `src/index.spec.ts` smoke test.

**AC5 verified live:** authored a throwaway `import { x } from "../../foo"` and ESLint
reported `no-restricted-imports` error with the Art. VI message; file removed.

**Verification output (real runs, exit codes):**
```
pnpm install      → Done in 6.4s (199 pkgs added, 0 errors)   exit 0   [AC1]
pnpm run lint     → Tasks: 12 successful, 12 total            exit 0   [AC2]
pnpm run typecheck→ Tasks: 12 successful, 12 total            exit 0   [AC2]
pnpm run test     → Tasks: 12 successful, 12 total (12 smoke) exit 0   [AC2]
pnpm run build    → Tasks: 12 successful, 12 total            exit 0   [AC2]
```

**AC coverage:** AC1 ✅ install clean · AC2 ✅ lint/typecheck/test (+build) exit 0 ·
AC3 ✅ `.github/workflows/ci.yml` + branch-protection doc · AC4 ✅ new package inherits
base tsconfig/eslint/vitest with zero reconfig (12 skeletons prove it) · AC5 ✅
`no-restricted-imports` enforced & verified.

**Notes / debt for @qa:**
- Branch protection (AC3 "blocks merge on failure") is a GitHub repo setting that cannot
  be committed — documented in `docs/framework/ci.md`; **@devops must apply** the required
  `ci` status check. This is the one AC3 element outside file scope.
- esbuild postinstall script was left unapproved by pnpm (vitest still runs fine via its
  prebuilt binary). If CI ever needs it, run `pnpm approve-builds`. Low risk.
- Coverage threshold intentionally NOT enforced (S0.0 ships smoke only; gate ≥90% from S0.1).
- No git repo initialized in workspace (clean dir); no commits made (per @devops authority).

### File List

**Root config (created):**
- `package.json`
- `pnpm-workspace.yaml`
- `tsconfig.base.json`
- `turbo.json`
- `eslint.config.js`
- `.prettierrc`
- `.prettierignore`
- `vitest.config.ts`
- `.npmrc`
- `pnpm-lock.yaml` (generated by `pnpm install`)

**CI (created):**
- `.github/workflows/ci.yml`

**Docs (created):**
- `docs/framework/ci.md`
- `apps/web/README.md` (placeholder, reserves S0.3 location)

> `docs/framework/{coding-standards,tech-stack,source-tree}.md` and the ADRs already
> existed (authored by @architect) — not modified by @dev.

**Package skeletons (created — 5 files each: `package.json`, `tsconfig.json`,
`vitest.config.ts`, `src/index.ts`, `src/index.spec.ts`):**
- `packages/core/`
- `packages/db/`
- `packages/auth/`
- `packages/ui/`
- `packages/llm-router/`
- `packages/cost/`
- `packages/artifacts/`
- `packages/orchestrator/`
- `packages/agents/`
- `packages/adapters/`
- `packages/kb/`
- `packages/events/`

**Story file (modified):** `docs/stories/0.0.bootstrap-monorepo.md` (Status, checkboxes,
Change Log, this Dev Agent Record).

## QA Results

### Review Date: 2026-06-23

### Reviewed By: Quinn (Test Architect & Quality Advisor)

### Verdict: **PASS**

All 5 acceptance criteria verified by live execution (not just from the Dev Agent Record).
Foundation scope respected per Constitution Art. IV — no out-of-scope SDKs leaked.

### Real command output (run by @qa from `C:/Projetos/BigFlux`)

```
pnpm install                       → "Already up to date", 13 workspace projects     exit 0   [AC1]
pnpm run lint                      → Tasks: 12 successful, 12 total (FULL TURBO)      exit 0   [AC2]
pnpm run typecheck                 → Tasks: 12 successful, 12 total (FULL TURBO)      exit 0   [AC2]
pnpm exec turbo run test --force   → Tasks: 12 successful, 12 total (no cache;
                                     12 smoke tests passed, 0 failures)               exit 0   [AC2]
```

AC5 verified live (not just reported): @qa authored a throwaway
`import { x } from "../../foo"` in `packages/core/src/`, ran `pnpm exec eslint` on it →
errored with `no-restricted-imports` and the exact Art. VI message, exit 1. Probe removed.
No application source code was modified by QA.

```
1:1  error  '../../foo' import is restricted ... Use the '@bigflux/*' path alias
            (Constitution Art. VI)  no-restricted-imports
✖ 1 problem (1 error, 0 warnings)   exit 1
```

### 7 Quality Checks

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | Code review (patterns/legibility/maintainability) | ✅ PASS | Config-only foundation; `tsconfig.base.json` strict + `noUncheckedIndexedAccess`; ESLint flat config clean; 12 skeletons consistent (each `package.json`/`tsconfig.json` extends base/`vitest.config.ts`/`src/index.ts`/smoke spec). |
| 2 | Unit tests | ✅ PASS | 12 smoke tests, all passing on a forced no-cache run. ≥90% coverage gate correctly NOT applied to S0.0 (foundation) — confirmed per Testing section + tech-stack §3.3. |
| 3 | Acceptance criteria (AC1–AC5) | ✅ PASS | AC1 install clean · AC2 lint/typecheck/test exit 0 · AC3 `.github/workflows/ci.yml` runs install→lint→typecheck→test (branch protection doc'd, @devops-pending — see REL-001) · AC4 new package inherits base tsconfig/eslint/vitest with zero reconfig (verified: `packages/core/tsconfig.json` extends `../../tsconfig.base.json`) · AC5 `no-restricted-imports` enforced (verified live). |
| 4 | No regressions | ✅ PASS | Greenfield foundation; nothing pre-existing to regress. |
| 5 | Performance | ✅ PASS | Within acceptable limits (full pipeline < 8s cold). |
| 6 | Security (OWASP basics) | ✅ PASS | No hardcoded secrets/credentials in repo config. LLM/Supabase SDKs NOT installed (no client-side key surface in foundation). `pnpm-lock.yaml` scanned: no `@supabase`/`react`/`xstate`/`@anthropic-ai`/`openai`/`@google/generative-ai`/`storybook`/`zod` direct deps. (`vite` present only transitively via Vitest 2 — not the S0.3 frontend Vite.) |
| 7 | Documentation | ✅ PASS | `docs/framework/{tech-stack,coding-standards,source-tree}.md` present (devLoadAlwaysFiles OK); `docs/framework/ci.md` documents pipeline + branch-protection handoff to @devops. |

### Scope compliance (Constitution Art. IV — No Invention)

✅ Only foundation tooling installed (TS 5.9, pnpm 10.26 + Turborepo 2.9, ESLint 9 flat + Prettier 3, Vitest 2 + coverage-v8). NO Supabase SDK / React / Vite-frontend / XState / LLM SDKs / Storybook / Zod — correctly deferred to owner stories S0.1+. File List matches the real filesystem (verified: 12 packages × 5 files + root config + CI + docs all present; `apps/web/README.md` placeholder reserves S0.3 location).

### Issues

| ID | Severity | Category | Description | Recommendation |
|----|----------|----------|-------------|----------------|
| REL-001 | low | reliability | Branch protection required status check `ci` (AC3 "blocks merge on failure") is a GitHub repo setting, not committable to the repo. Documented in `docs/framework/ci.md`. | @devops applies the required `ci` status check on `main` (Settings → Branches). Acceptable observation per QA brief — **not blocking**. |
| REL-002 | low | reliability | esbuild postinstall build script left unapproved by pnpm (`Ignored build scripts` warning). Vitest runs fine via prebuilt binary. | Run `pnpm approve-builds` if CI ever needs esbuild's native build. Low risk. |

No high/critical issues. Both findings are low-severity, pre-disclosed by @dev, and do not block.

### Gate Status

Gate: PASS → docs/qa/gates/0.0-bootstrap-monorepo.yml
